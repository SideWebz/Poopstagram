const Passport = require('passport');
const LocalStrategy = require('passport-local').Strategy;

// This file will be configured when you install dependencies
// For now, this is a placeholder

module.exports = Passport;
